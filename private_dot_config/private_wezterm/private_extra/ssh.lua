return {
   {
      -- This name identifies the domain
      name = 'rss',
      -- The hostname or address to connect to. Will be used to match settings
      -- from your ssh config file
      remote_address = 'rss.wyssmann.dev',
      -- The username to use on the remote host
      username = 'ansible',
      multiplexing = 'None',
  }
}
